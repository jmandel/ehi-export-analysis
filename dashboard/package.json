{
  "name": "ehi-dashboard",
  "private": true,
  "scripts": {
    "build": "bun run build-data.ts && bun build src/index.tsx --outdir dist --minify && cp index.html md.html style.css dist/",
    "dev": "bun build src/index.tsx --outdir dist --watch"
  },
  "dependencies": {
    "react": "^19",
    "react-dom": "^19",
    "react-markdown": "^9",
    "remark-gfm": "^4"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
